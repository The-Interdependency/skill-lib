# Current production-stable MCP Python SDK line as verified 2026-08-07.
# Re-check the official SDK/PyPI release before production installation; migrate
# to v2 once it is a stable PyPI release rather than a pre-release.
mcp>=1.28.1,<2
